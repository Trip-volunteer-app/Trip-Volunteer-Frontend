import { TripVolunteerFilterPipe } from './trip-volunteer-filter.pipe';

describe('TripVolunteerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TripVolunteerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
